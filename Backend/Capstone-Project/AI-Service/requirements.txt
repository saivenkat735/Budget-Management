# === Core web framework ===
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# === Validation & settings ===
pydantic>=2.9.2
pydantic-settings>=2.5.2

# === Auth (JWT - same HS256 as Spring Boot Secure service) ===
# PyJWT is actively maintained and Python 3.13-ready
PyJWT>=2.9.0
cryptography>=43.0.0

# === Vector store (course toolkit) ===
chromadb>=0.5.20

# === PDF parsing ===
pypdf>=5.0.1

# === Embeddings (free, local) ===
sentence-transformers>=3.2.0

# === LLM client (OpenAI-compatible -> points at Ollama locally) ===
openai>=1.51.0

# === HTTP client to call existing Spring services ===
httpx>=0.27.2

# === Relational storage (SQLite for chat history + document metadata) ===
sqlalchemy>=2.0.35
aiosqlite>=0.20.0

# === Numerics ===
numpy>=1.26.4
